apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - keycloak.yaml
configMapGenerator:
  - name: keycloak-realm
    files:
      - realm.json
generatorOptions:
  disableNameSuffixHash: true
