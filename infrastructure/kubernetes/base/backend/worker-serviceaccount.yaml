# Separate from `api`'s ServiceAccount (api-serviceaccount.yaml) — same reasoning: RBAC scoped
# per-workload rather than shared, even though neither needs any in-cluster Kubernetes API access
# today.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: worker
  namespace: smb-copilot
automountServiceAccountToken: false
