# No in-cluster Kubernetes API access is needed by the app itself; this
# exists so RBAC can be scoped per-workload instead of defaulting to
# `default` (and so `automountServiceAccountToken` can stay off).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
  namespace: smb-copilot
automountServiceAccountToken: false
