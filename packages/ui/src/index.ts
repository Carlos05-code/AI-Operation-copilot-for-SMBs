/**
 * @smb-copilot/ui — design tokens and WCAG utilities.
 *
 * Public surface: all token groups plus contrast helpers. Consumers import
 * from `@smb-copilot/ui` (JS/TS) or the generated artifacts for the target
 * platform (`generated/tokens.css`, `generated/tokens.dart`).
 */
export * from './tokens/index.js';
export * from './lib/contrast.js';
